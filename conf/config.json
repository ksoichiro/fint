{
  "rules": [
    {
      "pattern": "else{",
      "message": "Space must be inserted after else"
    },
    {
      "pattern": "}else",
      "message": "Space must be inserted before else"
    }
  ]
}

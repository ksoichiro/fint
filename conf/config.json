{
  "rulesets" : [
    {
      "id": "objc",
      "description": "Objective-C",
      "rules": [
        {
          "pattern": "else{",
          "message": {
            "default": "Space must be inserted after else",
            "ja": "elseの後にはスペースを入れてください"
          }
        },
        {
          "pattern": "}else",
          "message": {
            "default": "Space must be inserted before else",
            "ja": "elseの後にはスペースを入れてください"
          }
        }
      ]
    }
  ]
}
